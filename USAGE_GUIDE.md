# 📖 คู่มือการใช้งาน TorYod CMS Desktop App

## 🎯 วิธีใช้งานแอพกับเว็บไซต์อื่น

### ขั้นตอนที่ 1: เปิดแอพ

1. ดับเบิลคลิกไฟล์ `.exe` ที่อยู่ในโฟลเดอร์ `dist/`
2. รอให้แอพเปิดขึ้นมา

### ขั้นตอนที่ 2: กรอก URL ของเว็บไซต์

1. ในช่อง "Website URL" กรอก URL ของเว็บไซต์ที่ต้องการ
   - ตัวอย่าง: `https://example.com`
   - หรือ: `http://localhost:3000` (สำหรับเว็บ local)

2. คลิกปุ่ม **"🔍 ดึงข้อมูล"**

### ขั้นตอนที่ 3: ตรวจสอบข้อมูลที่ดึงได้

แอพจะแสดงข้อมูลที่ดึงได้:
- **📝 ข้อความ** - ข้อความทั้งหมดในเว็บไซต์
- **🎨 สี** - สีที่ใช้ในเว็บไซต์
- **🖼️ รูปภาพ** - รูปภาพทั้งหมด
- **💅 CSS Styles** - CSS ที่ใช้

### ขั้นตอนที่ 4: ติดตั้ง Plugin

1. ตรวจสอบข้อมูลที่ดึงได้ว่าถูกต้อง
2. คลิกปุ่ม **"⚡ ติดตั้ง Plugin"**
3. แอพจะ:
   - สร้าง plugin code
   - ติดตั้ง plugin ในเว็บไซต์
   - บันทึกข้อมูลที่ดึงได้

### ขั้นตอนที่ 5: ใช้ Plugin ในเว็บไซต์

หลังจากติดตั้ง plugin แล้ว:

1. เปิดเว็บไซต์ที่ติดตั้ง plugin
2. จะเห็นปุ่ม **"CMS Admin"** ที่มุมล่างขวา
3. คลิกปุ่มเพื่อเปิด Admin Panel
4. แก้ไขเนื้อหา, สี, รูปภาพได้ทันที

---

## 🌐 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: ใช้งานกับเว็บ Local

```
1. เปิดเว็บ local: http://localhost:5173
2. เปิดแอพ .exe
3. กรอก URL: http://localhost:5173
4. คลิก "ดึงข้อมูล"
5. คลิก "ติดตั้ง Plugin"
6. Refresh เว็บไซต์
7. จะเห็นปุ่ม "CMS Admin"
```

### ตัวอย่างที่ 2: ใช้งานกับเว็บ Production

```
1. เปิดแอพ .exe
2. กรอก URL: https://example.com
3. คลิก "ดึงข้อมูล"
4. ตรวจสอบข้อมูล
5. คลิก "ติดตั้ง Plugin"
6. เปิดเว็บไซต์ จะเห็นปุ่ม "CMS Admin"
```

---

## ⚙️ การตั้งค่า

### สำหรับเว็บ Local

- URL: `http://localhost:PORT`
- ตัวอย่าง: `http://localhost:3000`, `http://localhost:5173`

### สำหรับเว็บ Production

- URL: `https://your-domain.com`
- ต้องมี HTTPS (สำหรับความปลอดภัย)

---

## 🔧 การแก้ไขปัญหา

### ปัญหา: ดึงข้อมูลไม่ได้

**สาเหตุ:**
- เว็บไซต์มี CORS protection
- URL ไม่ถูกต้อง
- เว็บไซต์ไม่สามารถเข้าถึงได้

**แก้ไข:**
- ตรวจสอบ URL ว่าถูกต้อง
- ลองใช้เว็บ local ก่อน
- ตรวจสอบ internet connection

### ปัญหา: ติดตั้ง Plugin ไม่ได้

**สาเหตุ:**
- ไม่มีสิทธิ์แก้ไขเว็บไซต์
- เว็บไซต์มี security protection

**แก้ไข:**
- ใช้กับเว็บ local ก่อน
- ตรวจสอบว่าเว็บไซต์สามารถแก้ไขได้
- ลองใช้ manual installation (คัดลอกโค้ดเอง)

### ปัญหา: ไม่เห็นปุ่ม CMS Admin

**แก้ไข:**
1. Refresh หน้าเว็บ
2. ตรวจสอบว่า plugin code ถูกติดตั้งแล้ว
3. เปิด Console (F12) ดู error

---

## 📋 Checklist การใช้งาน

- [ ] เปิดแอพ .exe
- [ ] กรอก URL ของเว็บไซต์
- [ ] คลิก "ดึงข้อมูล"
- [ ] ตรวจสอบข้อมูลที่ดึงได้
- [ ] คลิก "ติดตั้ง Plugin"
- [ ] เปิดเว็บไซต์
- [ ] ตรวจสอบว่ามีปุ่ม "CMS Admin"
- [ ] คลิกปุ่มเพื่อเปิด Admin Panel

---

## 💡 Tips

1. **ทดสอบกับเว็บ Local ก่อน** - ง่ายและปลอดภัยกว่า
2. **ตรวจสอบข้อมูลก่อนติดตั้ง** - ดูว่าข้อมูลถูกต้องหรือไม่
3. **บันทึกข้อมูล** - แอพจะบันทึกข้อมูลอัตโนมัติ
4. **ใช้กับเว็บที่คุณมีสิทธิ์** - ไม่สามารถใช้กับเว็บที่ไม่มีสิทธิ์ได้

---

## 🎯 วิธีใช้งาน Plugin หลังติดตั้ง

### 1. เปิด Admin Panel

- คลิกปุ่ม "CMS Admin" ที่มุมล่างขวาของเว็บไซต์
- หรือไปที่ `https://your-domain.com/cms-admin.html`

### 2. แก้ไขเนื้อหา

- ไปที่ **Content Editor**
- เลือก element ที่ต้องการแก้ไข
- แก้ไขข้อความหรือ HTML

### 3. แก้ไขสี

- ไปที่ **CSS Editor**
- เพิ่ม Style Override
- เปลี่ยนสีตามต้องการ

### 4. แก้ไขรูปภาพ

- ไปที่ **Content Editor**
- เลือก element ที่มีรูปภาพ
- อัปโหลดรูปภาพใหม่

### 5. บันทึก

- คลิกปุ่ม **"💾 บันทึก"**
- การเปลี่ยนแปลงจะถูกใช้ทันที

---

**Version**: 1.0.0  
**Last Updated**: 2024
