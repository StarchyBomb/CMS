# 📦 คู่มือการติดตั้ง TorYod Universal CMS

## ภาพรวม

TorYod Universal CMS เป็นระบบจัดการเนื้อหาที่สามารถติดตั้งในเว็บไซต์ใดก็ได้ โดยไม่ต้องแก้ไขโค้ดเดิม สามารถแก้ไข CSS และ HTML ได้ผ่าน Admin Panel

## ✨ คุณสมบัติ

- ✅ **แก้ไข CSS** - เปลี่ยน styles ของ elements ใดก็ได้
- ✅ **แก้ไข Content** - เปลี่ยนข้อความและ HTML
- ✅ **Custom CSS** - เขียน CSS แบบกำหนดเอง
- ✅ **ไม่ต้องแก้โค้ด** - ติดตั้งได้ทันทีโดยไม่ต้องแก้ไข source code
- ✅ **Export/Import** - ส่งออกและนำเข้าการตั้งค่า
- ✅ **Real-time Preview** - เห็นผลลัพธ์ทันที

## 🚀 การติดตั้ง

### วิธีที่ 0: ติดตั้งแบบ One-Click (ง่ายที่สุด!) ⚡

**สำหรับผู้ที่ไม่ต้องการแก้ไขโค้ด:**

1. เปิดไฟล์ `setup.html` ใน browser
2. กรอกข้อมูลตามขั้นตอน (3 ขั้นตอน)
3. คัดลอกโค้ดที่สร้างให้
4. วางโค้ดในเว็บไซต์
5. เสร็จ! 🎉

**หรือใช้ Auto-Install Script:**

```html
<!-- เพิ่มโค้ดนี้ในหน้าเว็บ -->
<script src="install.js"></script>
<script>
  TorYodCMSInstaller.install({
    cmsPath: '/cms/',
    storageKey: 'toryod-cms-config'
  });
</script>
```

### วิธีที่ 1: ติดตั้งแบบ Standalone (แนะนำ)

1. **ดาวน์โหลดไฟล์**
   ```
   cms-widget.js
   cms-admin.html
   cms-admin.js
   cms-admin.css
   ```

2. **อัปโหลดไฟล์ไปยังเว็บไซต์**
   - อัปโหลดไฟล์ทั้งหมดไปยังโฟลเดอร์เดียวกัน (เช่น `/cms/`)

3. **เพิ่ม Script ในเว็บไซต์**
   
   เพิ่มโค้ดนี้ก่อน `</body>` ในทุกหน้าที่ต้องการใช้ CMS:
   
   ```html
   <!-- TorYod Universal CMS -->
   <script src="/cms/cms-widget.js"></script>
   <script>
     TorYodCMS.init({
       adminUrl: 'https://your-domain.com/cms/cms-admin.html',
       storageKey: 'toryod-cms-config'
     });
   </script>
   ```

4. **เปิด Admin Panel**
   - ไปที่ `https://your-domain.com/cms/cms-admin.html`
   - หรือคลิกปุ่ม "CMS Admin" ที่มุมล่างขวาของเว็บไซต์

### วิธีที่ 2: ติดตั้งผ่าน CDN

```html
<!-- TorYod Universal CMS -->
<script src="https://cdn.your-domain.com/cms-widget.js"></script>
<script>
  TorYodCMS.init({
    adminUrl: 'https://your-domain.com/cms/cms-admin.html',
    storageKey: 'toryod-cms-config'
  });
</script>
```

### วิธีที่ 3: ติดตั้งผ่าน npm (ถ้ามี)

```bash
npm install toryod-cms
```

```javascript
import TorYodCMS from 'toryod-cms';

TorYodCMS.init({
  adminUrl: 'https://your-domain.com/cms/cms-admin.html',
  storageKey: 'toryod-cms-config'
});
```

## 📝 การใช้งาน

### 1. แก้ไข CSS

**วิธีที่ 1: Custom CSS**
1. ไปที่ **CSS Editor** → **Custom CSS**
2. เขียน CSS ของคุณ
3. คลิก **ใช้ CSS**

**วิธีที่ 2: Style Overrides**
1. ไปที่ **CSS Editor** → **Style Overrides**
2. คลิก **+ เพิ่ม Style Override**
3. ใส่ CSS Selector (เช่น `.header`, `#logo`)
4. ใส่ Property (เช่น `color`, `font-size`)
5. ใส่ Value (เช่น `red`, `24px`)
6. คลิก **บันทึก**

### 2. แก้ไข Content

1. ไปที่ **Content Editor**
2. คลิก **+ เพิ่ม Content Rule**
3. ใส่ CSS Selector
4. เลือกประเภท (Text หรือ HTML)
5. ใส่เนื้อหาที่ต้องการ
6. คลิก **บันทึก**

### 3. Export/Import Configuration

**Export:**
1. คลิก **📥 Export**
2. ไฟล์ JSON จะถูกดาวน์โหลด

**Import:**
1. คลิก **📤 Import**
2. เลือกไฟล์ JSON ที่ export ไว้
3. ระบบจะโหลดการตั้งค่าทั้งหมด

## 🎯 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: เปลี่ยนสี Header

```javascript
// ใน Admin Panel → CSS Editor → Style Overrides
Selector: .header
Property: background-color
Value: #FF8C5A
```

### ตัวอย่างที่ 2: เปลี่ยนข้อความ Title

```javascript
// ใน Admin Panel → Content Editor
Selector: h1.title
Type: Text
Content: ข้อความใหม่
```

### ตัวอย่างที่ 3: เพิ่ม Custom CSS

```css
/* ใน Admin Panel → CSS Editor → Custom CSS */
.header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    box-shadow: 0 10px 30px rgba(0,0,0,0.3);
}

.button {
    border-radius: 50px;
    padding: 15px 30px;
    transition: all 0.3s;
}
```

## ⚙️ Configuration Options

```javascript
TorYodCMS.init({
    // URL ของ Admin Panel
    adminUrl: 'https://your-domain.com/cms/cms-admin.html',
    
    // Key สำหรับเก็บข้อมูลใน localStorage
    storageKey: 'toryod-cms-config',
    
    // API Key (ถ้ามี backend)
    apiKey: 'optional-api-key'
});
```

## 🔧 API Methods

### สำหรับ Developer

```javascript
// ตั้งค่า Style
TorYodCMS.setStyle('.header', 'background-color', 'red');

// ตั้งค่า Content
TorYodCMS.setContent('.title', 'New Title', 'text');

// ตั้งค่า Custom CSS
TorYodCMS.setCustomCSS('body { background: red; }');

// Export Config
const config = TorYodCMS.exportConfig();

// Import Config
TorYodCMS.importConfig(jsonString);

// Reset
TorYodCMS.reset();
```

## 🛠️ Troubleshooting

### ปัญหา: CMS ไม่ทำงาน

**แก้ไข:**
1. ตรวจสอบว่า script ถูกโหลดแล้ว
2. เปิด Console (F12) ดู error
3. ตรวจสอบว่า `TorYodCMS` ถูก define แล้ว

### ปัญหา: Styles ไม่ถูก apply

**แก้ไข:**
1. ตรวจสอบ CSS Selector ว่าถูกต้อง
2. ตรวจสอบว่า element มีอยู่ในหน้าเว็บ
3. ลองใช้ `!important` ใน value

### ปัญหา: Content ไม่เปลี่ยน

**แก้ไข:**
1. ตรวจสอบว่า Selector ถูกต้อง
2. ตรวจสอบว่า element มีอยู่ในหน้าเว็บ
3. รอให้ DOM โหลดเสร็จก่อน

## 📚 เอกสารเพิ่มเติม

- [API Reference](./CMS_API_REFERENCE.md)
- [Architecture](./CMS_ARCHITECTURE.md)
- [Documentation](./CMS_DOCUMENTATION.md)

## 🤝 Support

หากมีปัญหาหรือคำถาม:
- สร้าง Issue ใน GitHub
- ติดต่อทีมพัฒนา

## 📄 License

MIT License

---

**Version**: 1.0.0  
**Last Updated**: 2024
