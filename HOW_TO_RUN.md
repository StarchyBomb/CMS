# 🚀 วิธีรันแอพ TorYod CMS

## วิธีที่ 1: ใช้ Script (ง่ายที่สุด!)

### Windows:
```bash
# ดับเบิลคลิกไฟล์
run-app.bat

# หรือรันจาก Command Prompt
run-app.bat
```

### Linux/Mac:
```bash
# ให้สิทธิ์ execute
chmod +x run-app.sh

# รัน
./run-app.sh
```

---

## วิธีที่ 2: ใช้ http-server

### ติดตั้ง http-server (ครั้งเดียว):
```bash
npm install -g http-server
```

### รันแอพ:
```bash
# วิธีที่ 1: เปิดอัตโนมัติ
http-server -p 8080 -o cms-app.html

# วิธีที่ 2: เปิดเอง
http-server -p 8080
# แล้วเปิด browser ไปที่: http://localhost:8080/cms-app.html
```

---

## วิธีที่ 3: เปิดไฟล์โดยตรง

### เปิดใน Browser:
1. ดับเบิลคลิกที่ไฟล์ `cms-app.html`
2. หรือคลิกขวา → Open with → Browser

**หมายเหตุ:** วิธีนี้อาจมีข้อจำกัดบางอย่าง (เช่น Service Worker อาจไม่ทำงาน)

---

## วิธีที่ 4: ใช้ Python (ถ้ามี Python)

```bash
# Python 3
python -m http.server 8080

# Python 2
python -m SimpleHTTPServer 8080

# แล้วเปิด browser ไปที่: http://localhost:8080/cms-app.html
```

---

## วิธีที่ 5: ใช้ Electron (Desktop App)

### ติดตั้ง Dependencies:
```bash
npm install
```

### รันแอพ:
```bash
npm start
```

### Build สำหรับ Production:
```bash
npm run build
```

---

## วิธีที่ 6: ใช้ Live Server (VS Code Extension)

1. ติดตั้ง Extension "Live Server" ใน VS Code
2. คลิกขวาที่ไฟล์ `cms-app.html`
3. เลือก "Open with Live Server"

---

## 📋 Checklist

- [ ] Node.js ติดตั้งแล้ว (สำหรับ http-server)
- [ ] เปิดไฟล์ `cms-app.html` หรือรัน script
- [ ] Browser เปิดที่ `http://localhost:8080/cms-app.html`
- [ ] แอพทำงานปกติ

---

## ❓ ปัญหาที่พบบ่อย

### ปัญหา: Node.js ไม่ได้ติดตั้ง

**แก้ไข:**
- ดาวน์โหลดและติดตั้งจาก https://nodejs.org
- หรือใช้วิธีที่ 3 (เปิดไฟล์โดยตรง)

### ปัญหา: Port 8080 ถูกใช้แล้ว

**แก้ไข:**
```bash
# ใช้ port อื่น
http-server -p 3000 -o cms-app.html
```

### ปัญหา: Service Worker ไม่ทำงาน

**แก้ไข:**
- ต้องใช้ local server (ไม่ใช่ file://)
- ใช้ http://localhost:8080

---

## 🎯 แนะนำ

**สำหรับผู้ใช้ทั่วไป:**
- ใช้ `run-app.bat` (Windows) หรือ `run-app.sh` (Linux/Mac)

**สำหรับ Developer:**
- ใช้ `http-server` หรือ `npm start` (Electron)

---

**พร้อมใช้งานแล้ว!** 🎉
