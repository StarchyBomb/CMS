# ⚡ Quick Start Guide - TorYod Universal CMS

## เริ่มต้นใช้งานใน 5 นาที

### 🎯 วิธีที่ 1: ใช้ Setup Wizard (ง่ายที่สุด!)

1. **เปิดไฟล์ `setup.html`** (30 วินาที)
   - ดับเบิลคลิกที่ไฟล์ หรือเปิดใน browser

2. **ทำตามขั้นตอน** (2 นาที)
   - กรอกข้อมูล (URL, Path)
   - คัดลอกโค้ด
   - อัปโหลดไฟล์ (ถ้าต้องการ)

3. **เสร็จ!** (30 วินาที)
   - วางโค้ดในเว็บไซต์
   - เปิด Admin Panel

**ดูรายละเอียดที่ [AUTO_INSTALL.md](./AUTO_INSTALL.md)**

---

### วิธีที่ 2: ติดตั้งแบบ Manual

### ขั้นตอนที่ 1: เตรียมไฟล์ (1 นาที)

ดาวน์โหลดหรือคัดลอกไฟล์เหล่านี้:
- `cms-widget.js`
- `cms-admin.html`
- `cms-admin.js`
- `cms-admin.css`

### ขั้นตอนที่ 2: ติดตั้งในเว็บไซต์ (2 นาที)

เพิ่มโค้ดนี้ก่อน `</body>` ในเว็บไซต์ของคุณ:

```html
<!-- TorYod Universal CMS -->
<script src="cms-widget.js"></script>
<script>
  TorYodCMS.init({
    adminUrl: 'cms-admin.html',
    storageKey: 'toryod-cms-config'
  });
</script>
```

**หมายเหตุ:** เปลี่ยน path ให้ตรงกับตำแหน่งไฟล์ของคุณ

### ขั้นตอนที่ 3: เปิด Admin Panel (30 วินาที)

1. เปิดไฟล์ `cms-admin.html` ใน browser
2. หรือคลิกปุ่ม "CMS Admin" ที่มุมล่างขวาของเว็บไซต์

### ขั้นตอนที่ 4: เริ่มแก้ไข! (1 นาที)

#### ตัวอย่างที่ 1: เปลี่ยนสี Header

1. ไปที่ **CSS Editor** → **Style Overrides**
2. คลิก **+ เพิ่ม Style Override**
3. ใส่:
   - Selector: `.header` (หรือ selector อื่น)
   - Property: `background-color`
   - Value: `#FF8C5A`
4. คลิก **บันทึก**
5. เปิดเว็บไซต์ดูผลลัพธ์!

#### ตัวอย่างที่ 2: เปลี่ยนข้อความ

1. ไปที่ **Content Editor**
2. คลิก **+ เพิ่ม Content Rule**
3. ใส่:
   - Selector: `h1.title` (หรือ selector อื่น)
   - Type: `Text`
   - Content: `ข้อความใหม่`
4. คลิก **บันทึก**
5. เปิดเว็บไซต์ดูผลลัพธ์!

#### ตัวอย่างที่ 3: เขียน Custom CSS

1. ไปที่ **CSS Editor** → **Custom CSS**
2. เขียน CSS:
   ```css
   .header {
       background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
       box-shadow: 0 10px 30px rgba(0,0,0,0.3);
   }
   ```
3. คลิก **ใช้ CSS**
4. เปิดเว็บไซต์ดูผลลัพธ์!

## 🎯 Tips

### หา CSS Selector

1. เปิดเว็บไซต์ใน browser
2. กด F12 เพื่อเปิด Developer Tools
3. คลิกไอคอน "Inspect Element"
4. คลิกที่ element ที่ต้องการ
5. ดูที่ Elements tab - จะเห็น selector

**ตัวอย่าง Selectors:**
- `.header` - class="header"
- `#logo` - id="logo"
- `h1` - tag h1
- `.nav a` - link ภายใน nav
- `#header .logo` - logo ภายใน header

### Export/Import

**Export:** คลิก **📥 Export** เพื่อบันทึกการตั้งค่า

**Import:** คลิก **📤 Import** เพื่อโหลดการตั้งค่า

## ❓ ปัญหาที่พบบ่อย

**Q: CMS ไม่ทำงาน**
- ตรวจสอบว่า script ถูกโหลดแล้ว
- เปิด Console (F12) ดู error
- ตรวจสอบ path ของไฟล์

**Q: Styles ไม่ถูก apply**
- ตรวจสอบ CSS Selector ว่าถูกต้อง
- ลองใช้ `!important` ใน value
- ตรวจสอบว่า element มีอยู่ในหน้าเว็บ

**Q: Content ไม่เปลี่ยน**
- ตรวจสอบ Selector ว่าถูกต้อง
- รอให้ DOM โหลดเสร็จก่อน

## 🚀 พร้อมแล้ว!

ตอนนี้คุณสามารถแก้ไขเว็บไซต์ได้โดยไม่ต้องแก้โค้ดแล้ว!

สำหรับข้อมูลเพิ่มเติม ดูที่ [INSTALLATION.md](./INSTALLATION.md)
