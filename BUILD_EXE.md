# 🔨 Build .exe สำหรับ TorYod CMS

## วิธี Build .exe

### Windows (ง่ายที่สุด!)

```bash
# ดับเบิลคลิกไฟล์
build.bat

# หรือรันจาก Command Prompt
build.bat
```

### Manual Build

```bash
# 1. ติดตั้ง dependencies
npm install

# 2. Build สำหรับ Windows
npm run build:win

# 3. ไฟล์ .exe จะอยู่ในโฟลเดอร์ dist/
```

## 📦 Dependencies

แอพต้องการ:
- Node.js (https://nodejs.org)
- npm (มาพร้อมกับ Node.js)

## 🚀 วิธีใช้งาน

### 1. Build .exe

```bash
build.bat
```

### 2. เปิดแอพ

- ไปที่โฟลเดอร์ `dist/`
- ดับเบิลคลิกไฟล์ `.exe`
- แอพจะเปิดขึ้นมา

### 3. ใช้งาน

1. **กรอก URL** ของเว็บไซต์ที่ต้องการดึงข้อมูล
2. **คลิก "ดึงข้อมูล"** - แอพจะดึง HTML, CSS, ข้อความ, สี, รูป
3. **คลิก "ติดตั้ง Plugin"** - แอพจะติดตั้ง plugin ในเว็บไซต์

## ✨ คุณสมบัติ

- ✅ **ดึงข้อมูลอัตโนมัติ** - ดึง HTML, CSS, ข้อความ, สี, รูปภาพ
- ✅ **ติดตั้ง Plugin** - ติดตั้ง CMS plugin ในเว็บไซต์
- ✅ **บันทึกข้อมูล** - บันทึกข้อมูลที่ดึงได้เป็น JSON
- ✅ **Standalone** - ทำงานได้เองโดยไม่ต้องพึ่ง browser

## 🔧 Troubleshooting

### ปัญหา: npm install ล้มเหลว

**แก้ไข:**
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules package-lock.json
npm install
```

### ปัญหา: Build ล้มเหลว

**แก้ไข:**
- ตรวจสอบว่า Node.js ติดตั้งแล้ว
- ตรวจสอบว่า dependencies ติดตั้งครบ
- ลอง `npm install` อีกครั้ง

### ปัญหา: .exe ไม่ทำงาน

**แก้ไข:**
- ตรวจสอบว่าไฟล์ครบถ้วน
- ลอง build ใหม่
- ตรวจสอบ antivirus (อาจบล็อกไฟล์)

---

**Version**: 1.0.0  
**Last Updated**: 2024
