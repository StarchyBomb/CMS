# 🎨 TorYod CMS Installer App

แอพติดตั้ง CMS สำหรับเว็บไซต์ Local - อัปโหลดไฟล์และติดตั้งได้ด้วยตัวเอง

## ✨ คุณสมบัติ

- ✅ **อัปโหลดไฟล์** - ลากวางหรือเลือกไฟล์ CMS
- ✅ **ตรวจสอบไฟล์** - ตรวจสอบว่ามีไฟล์ครบหรือไม่
- ✅ **ติดตั้งอัตโนมัติ** - ติดตั้ง CMS ในเว็บไซต์ Local
- ✅ **Local Only** - ทำงานได้เฉพาะกับเว็บไซต์ Local เท่านั้น
- ✅ **Generate Code** - สร้างโค้ดติดตั้งอัตโนมัติ

## 🚀 วิธีใช้งาน

### 1. เปิดแอพ

```bash
# วิธีที่ 1: ใช้ http-server
npx http-server -p 8080 -o app.html

# วิธีที่ 2: เปิดไฟล์โดยตรง
# เปิด app.html ใน browser
```

### 2. อัปโหลดไฟล์ CMS

1. ลากไฟล์ CMS มาวางในพื้นที่อัปโหลด
2. หรือคลิกเพื่อเลือกไฟล์
3. ไฟล์ที่ต้องการ:
   - `cms-widget.js`
   - `cms-admin.html`
   - `cms-admin.js`
   - `cms-admin.css`

### 3. ตั้งค่า

- **Website Path**: URL ของเว็บไซต์ Local (เช่น `http://localhost:5173`)
- **CMS Path**: Path ที่จะติดตั้งไฟล์ (เช่น `/cms/`)
- **Storage Key**: Key สำหรับเก็บข้อมูล

### 4. ติดตั้ง

1. คลิกปุ่ม "ติดตั้ง CMS"
2. รอให้การติดตั้งเสร็จ
3. คัดลอกโค้ดที่สร้างให้
4. วางโค้ดในเว็บไซต์ของคุณ

## 🔒 Local Only

แอพนี้จะตรวจสอบว่าเชื่อมต่อกับ Local หรือไม่:

- ✅ `localhost`
- ✅ `127.0.0.1`
- ✅ `192.168.x.x` (Local Network)
- ❌ Remote domains (จะแสดงคำเตือน)

## 📋 ไฟล์ที่ต้องการ

### ไฟล์ที่จำเป็น (Required):
- `cms-widget.js` - Widget script
- `cms-admin.html` - Admin Panel
- `cms-admin.js` - Admin Logic
- `cms-admin.css` - Admin Styles

### ไฟล์เสริม (Optional):
- `setup.html` - Setup Wizard
- `install.js` - Auto-install script

## 🛠️ Development

### Local Development

```bash
# เปิดแอพ
npx http-server -p 8080 -o app.html

# หรือใช้ Python
python -m http.server 8080
```

### Build for Production

```bash
# Install dependencies
npm install

# Start app
npm start
```

## 📦 Deployment

### วิธีที่ 1: Static Hosting

อัปโหลดไฟล์ `app.html` ไปยัง:
- GitHub Pages
- Netlify
- Vercel
- หรือ static hosting อื่นๆ

### วิธีที่ 2: Local Server

```bash
# ใช้ http-server
npx http-server -p 8080

# หรือใช้ Python
python -m http.server 8080
```

## ⚠️ ข้อจำกัด

1. **Local Only** - ทำงานได้เฉพาะกับเว็บไซต์ Local
2. **File Upload** - ต้องอัปโหลดไฟล์ด้วยตนเอง
3. **No Backend** - ไม่มี backend server (client-side only)

## 🔄 อัพเดท

```bash
# Pull อัพเดท
git pull origin main

# หรือใช้ update script
bash update.sh
```

## 📚 เอกสารเพิ่มเติม

- [README.md](./README.md) - เอกสารหลัก
- [INSTALLATION.md](./INSTALLATION.md) - คู่มือติดตั้ง
- [AUTO_INSTALL.md](./AUTO_INSTALL.md) - ติดตั้งอัตโนมัติ

---

**Version**: 1.0.0  
**Last Updated**: 2024
