# ⚡ One-Click Installation Guide

## วิธีติดตั้งแบบง่ายที่สุด (กดไม่กี่ปุ่ม)

### 🎯 วิธีที่ 1: ใช้ Setup Wizard (แนะนำ)

1. **เปิดไฟล์ `setup.html`**
   - ดับเบิลคลิกที่ไฟล์ `setup.html`
   - หรือเปิดใน browser

2. **กรอกข้อมูล (ขั้นตอนที่ 1)**
   - URL ของเว็บไซต์ (ระบบจะตรวจจับอัตโนมัติ)
   - ตำแหน่งไฟล์ CMS (เช่น `/cms/`)
   - Storage Key (ไม่บังคับ)

3. **คัดลอกโค้ด (ขั้นตอนที่ 2)**
   - คลิกปุ่ม "📋 คัดลอก"
   - วางโค้ดก่อน `</body>` ในเว็บไซต์

4. **อัปโหลดไฟล์ (ขั้นตอนที่ 3)**
   - ลากไฟล์มาวางหรือเลือกไฟล์
   - คลิก "เสร็จสิ้น"

5. **เสร็จ!** 🎉
   - คลิก "เปิด Admin Panel" เพื่อเริ่มใช้งาน

---

### 🚀 วิธีที่ 2: Auto-Install Script

**สำหรับผู้ที่ต้องการติดตั้งอัตโนมัติ:**

#### ขั้นตอนที่ 1: เพิ่ม Script

เพิ่มโค้ดนี้ในหน้าเว็บของคุณ (ก่อน `</body>`):

```html
<!-- TorYod CMS Auto-Install -->
<script src="install.js"></script>
<script>
  TorYodCMSInstaller.install({
    cmsPath: '/cms/',  // ตำแหน่งไฟล์ CMS
    storageKey: 'toryod-cms-config'
  });
</script>
```

#### ขั้นตอนที่ 2: อัปโหลดไฟล์

อัปโหลดไฟล์เหล่านี้ไปยัง `/cms/`:
- `cms-widget.js`
- `cms-admin.html`
- `cms-admin.js`
- `cms-admin.css`
- `install.js` (ถ้าใช้ auto-install)

#### ขั้นตอนที่ 3: เสร็จ!

ระบบจะติดตั้งอัตโนมัติเมื่อหน้าเว็บโหลด

---

### 🎨 วิธีที่ 3: แสดงปุ่มติดตั้งอัตโนมัติ

**สำหรับผู้ที่ต้องการให้ผู้ใช้ติดตั้งเอง:**

```html
<script src="install.js"></script>
<script>
  // แสดงปุ่มติดตั้ง (ถ้ายังไม่ได้ติดตั้ง)
  TorYodCMSInstaller.showInstallButton({
    cmsPath: '/cms/',
    storageKey: 'toryod-cms-config'
  });
</script>
```

ผู้ใช้จะเห็นปุ่ม "ติดตั้ง CMS" ที่มุมล่างขวา และสามารถคลิกเพื่อติดตั้งได้ทันที!

---

## 📋 Checklist การติดตั้ง

- [ ] อัปโหลดไฟล์ CMS ไปยังเว็บไซต์
- [ ] เพิ่มโค้ดติดตั้งในหน้าเว็บ
- [ ] เปิด Admin Panel เพื่อทดสอบ
- [ ] ทดสอบแก้ไข CSS และ Content

---

## ❓ ปัญหาที่พบบ่อย

**Q: Setup Wizard ไม่ทำงาน**
- ตรวจสอบว่าเปิดไฟล์ผ่าน HTTP/HTTPS (ไม่ใช่ file://)
- ใช้ local server เช่น `python -m http.server 8000`

**Q: Auto-install ไม่ทำงาน**
- ตรวจสอบ path ของไฟล์ว่าถูกต้อง
- เปิด Console (F12) ดู error messages
- ตรวจสอบว่าไฟล์ `cms-widget.js` ถูกอัปโหลดแล้ว

**Q: ปุ่มติดตั้งไม่แสดง**
- ตรวจสอบว่า CMS ยังไม่ได้ติดตั้งอยู่แล้ว
- ตรวจสอบ path ของ `install.js`

---

## 🎉 พร้อมแล้ว!

ตอนนี้คุณสามารถติดตั้ง TorYod CMS ได้ง่ายๆ แค่กดไม่กี่ปุ่ม!

สำหรับข้อมูลเพิ่มเติม ดูที่ [INSTALLATION.md](./INSTALLATION.md)
