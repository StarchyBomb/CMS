# 🔄 คู่มือการอัพเดท TorYod Universal CMS

## วิธีอัพเดท Repository ที่ Clone ไว้

### วิธีที่ 1: ใช้ Update Script (แนะนำ)

#### สำหรับ Linux/Mac:
```bash
bash update.sh
# หรือ
./update.sh
```

#### สำหรับ Windows (PowerShell):
```powershell
.\update.ps1
```

**Update Script จะ:**
- ✅ ตรวจสอบว่ามีอัพเดทหรือไม่
- ✅ เก็บการเปลี่ยนแปลงที่ยังไม่ได้ commit (ถ้ามี)
- ✅ ดึงอัพเดทจาก GitHub
- ✅ คืนค่าการเปลี่ยนแปลงที่เก็บไว้ (ถ้าต้องการ)

---

### วิธีที่ 2: ใช้ Git Command โดยตรง

#### อัพเดทแบบง่าย:
```bash
git pull origin main
```

#### อัพเดทแบบปลอดภัย (เก็บการเปลี่ยนแปลง):
```bash
# 1. เก็บการเปลี่ยนแปลงที่ยังไม่ได้ commit
git stash

# 2. ดึงอัพเดท
git pull origin main

# 3. คืนค่าการเปลี่ยนแปลง (ถ้าต้องการ)
git stash pop
```

#### อัพเดทแบบตรวจสอบก่อน:
```bash
# 1. ดูว่ามีอัพเดทหรือไม่
git fetch origin

# 2. เปรียบเทียบกับเวอร์ชันปัจจุบัน
git log HEAD..origin/main

# 3. ถ้ามีอัพเดท ให้ pull
git pull origin main
```

---

### วิธีที่ 3: อัพเดทแบบ Force (ระวัง!)

**⚠️ คำเตือน:** วิธีนี้จะทับการเปลี่ยนแปลงทั้งหมด!

```bash
# 1. เก็บการเปลี่ยนแปลง (ถ้าต้องการ)
git stash

# 2. Reset และ pull
git fetch origin
git reset --hard origin/main

# 3. คืนค่าการเปลี่ยนแปลง (ถ้าต้องการ)
git stash pop
```

---

## 📋 ขั้นตอนการอัพเดทแบบละเอียด

### 1. ตรวจสอบสถานะปัจจุบัน

```bash
# ดูว่ามีการเปลี่ยนแปลงหรือไม่
git status

# ดู commit history
git log --oneline -5
```

### 2. เก็บการเปลี่ยนแปลง (ถ้ามี)

```bash
# ดูการเปลี่ยนแปลง
git diff

# เก็บการเปลี่ยนแปลง
git stash push -m "My changes before update"
```

### 3. ดึงอัพเดท

```bash
# Fetch อัพเดท
git fetch origin

# ดูว่ามีอัพเดทอะไรบ้าง
git log HEAD..origin/main --oneline

# Pull อัพเดท
git pull origin main
```

### 4. ตรวจสอบการเปลี่ยนแปลง

```bash
# ดู commit ใหม่
git log --oneline -5

# ดูไฟล์ที่เปลี่ยน
git diff HEAD~1

# ดูไฟล์ที่เพิ่ม/ลบ
git diff --stat HEAD~1
```

### 5. คืนค่าการเปลี่ยนแปลง (ถ้าต้องการ)

```bash
# ดู stash list
git stash list

# คืนค่าการเปลี่ยนแปลงล่าสุด
git stash pop

# หรือคืนค่าเฉพาะ stash ที่ต้องการ
git stash apply stash@{0}
```

---

## 🔧 แก้ไขปัญหา

### ปัญหา: มี Conflict

**สาเหตุ:** การเปลี่ยนแปลงของคุณขัดแย้งกับอัพเดทใหม่

**แก้ไข:**
```bash
# 1. ดูไฟล์ที่มี conflict
git status

# 2. แก้ไขไฟล์ที่มี conflict
# (แก้ไขส่วนที่ระบุ <<<<<<< HEAD)

# 3. Add ไฟล์ที่แก้ไขแล้ว
git add <file>

# 4. Commit
git commit -m "Resolve merge conflict"
```

### ปัญหา: Pull ไม่ได้ (Diverged)

**สาเหตุ:** มี commit ที่แตกต่างกัน

**แก้ไข:**
```bash
# 1. ดูความแตกต่าง
git log --oneline --graph --all

# 2. Merge หรือ Rebase
git pull --rebase origin main
# หรือ
git pull origin main
```

### ปัญหา: Remote ไม่ถูกต้อง

**แก้ไข:**
```bash
# ดู remote ปัจจุบัน
git remote -v

# เปลี่ยน remote URL
git remote set-url origin https://github.com/StarchyBomb/CMS.git

# หรือเพิ่ม remote ใหม่
git remote add origin https://github.com/StarchyBomb/CMS.git
```

---

## 🔄 อัพเดทอัตโนมัติ

### สร้าง Cron Job (Linux/Mac)

```bash
# แก้ไข crontab
crontab -e

# เพิ่มบรรทัดนี้ (อัพเดททุกวันเวลา 2:00 น.)
0 2 * * * cd /path/to/CMS && git pull origin main
```

### สร้าง Scheduled Task (Windows)

1. เปิด Task Scheduler
2. สร้าง Basic Task ใหม่
3. ตั้งค่าให้รัน script:
   ```powershell
   cd C:\path\to\CMS
   git pull origin main
   ```

---

## 📊 ตรวจสอบเวอร์ชัน

### ดู commit ล่าสุด

```bash
git log -1 --pretty=format:"%h - %an, %ar : %s"
```

### ดู tag (ถ้ามี)

```bash
git tag
git describe --tags
```

### ดู branch

```bash
git branch -a
```

---

## 💡 Tips

1. **อัพเดทบ่อยๆ** - เพื่อให้ได้ bug fixes และ features ใหม่
2. **เก็บการเปลี่ยนแปลงก่อนอัพเดท** - ใช้ `git stash` เสมอ
3. **ตรวจสอบก่อน merge** - ใช้ `git fetch` และ `git log` ก่อน `git pull`
4. **Backup ก่อนอัพเดท** - สำรองไฟล์สำคัญก่อนอัพเดท

---

## 📚 เอกสารเพิ่มเติม

- [Git Documentation](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [README.md](./README.md) - เอกสารหลัก

---

**Version**: 1.0.0  
**Last Updated**: 2024
